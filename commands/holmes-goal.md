---
description: "Structure a task as a HOLMES-informed /goal objective"
argument-hint: "<what you want to achieve>"
---

Convert this raw goal intent into a HOLMES-informed `/goal` objective:

$ARGUMENTS

Apply HOLMES TARGET extraction before drafting:

1. Identify the true target outcome, not just the requested activity.
2. Define bounded acceptance criteria that make completion observable.
3. Identify key variables, dependencies, assumptions, and risks.
4. Separate must-have constraints from optional preferences.
5. Produce a concise `/goal` objective statement ready to paste, including:
   - objective
   - context
   - constraints
   - acceptance criteria
   - risks or open questions

Return only the structured `/goal` objective and any essential notes needed to use it safely.